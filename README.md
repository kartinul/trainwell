# Trainwell

so that you train well. mostly ui focused. very ehh

## Setup

Before building the app, you must add your Gemini API key to `Secrets.swift`:

```swift
struct Secrets {
    static let geminiAPIKey = "YOUR_API_KEY_HERE"
}
```
